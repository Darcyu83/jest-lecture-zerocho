{
  "scripts": {
    "test": "jest",
    "testWatch": "jest --watch",
    "testWatchAll": "jest --watchAll",
    "runTestInSingleThread": "jest --runInBand",
    "checkWhichTestHolding": "jest --detectOpenHandles",
    "runWithSpecificThreads": "jest --maxWorkers=50%"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "jest-extended": "^4.0.2",
    "ts-jest": "^29.3.0"
  },
  "dependencies": {
    "axios": "^1.8.4"
  }
}
